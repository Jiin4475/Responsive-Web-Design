const myName = 'Max';

if(myName === 'Max'){
    console.log('hello!');  
}

//isLoggedIn 현재 웹사이트에 방문자가 로그인 했는지 안 했는지 
let isLoggedIn = true;

if(!isLoggedIn){
    console.log('User is NOT logged in!');
}

const enteredUserName = 'Max';

if(enteredUserName.length > 0){
    console.log('input is valid!')
}